# ── LLM settings ────────────────────────────────────────────────────────
# Provide at least one key to enable LLM re-ranking.
# Without a key the system falls back to rule-based scoring.
HF_TOKEN=
OPENROUTER_API_KEY=
LLM_MODEL=moonshotai/Kimi-K2.5:novita
LLM_BASE_URL=https://router.huggingface.co/v1/chat/completions

# ── Embedding settings ──────────────────────────────────────────────────
EMBEDDING_PROVIDER=local
EMBEDDING_MODEL_LOCAL=all-MiniLM-L6-v2
# EMBEDDING_MODEL_API=text-embedding-ada-002
# EMBEDDING_API_BASE_URL=https://api.openai.com/v1/embeddings
# EMBEDDING_API_KEY=
